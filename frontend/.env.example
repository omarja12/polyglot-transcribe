# Copy to frontend/.env.local (or set env vars in your hosting provider)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Notes:
# - Do not store secrets in frontend env files. The frontend communicates with the backend API which holds the GROQ_API_KEY.
# - Start the frontend with `npm run dev` and the backend with uvicorn as documented in the repo README.
